{
  "name": "example",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json --watch",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.28.1",
    "@sveltejs/kit": "^1.5.0",
    "@ubermanu/sveltekit-websocket": "workspace:*",
    "svelte": "^3.54.0",
    "svelte-check": "^3.0.1",
    "typescript": "^5.0.0",
    "vite": "^4.3.0"
  },
  "type": "module"
}
